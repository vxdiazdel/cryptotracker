import React from 'react';

const Footer = () => (
  <footer className="footer">
    <p className="text-center">
      <small>&copy;{new Date().getFullYear()} CryptoTracker</small>
    </p>
  </footer>
);

export default Footer;
